netcdf cf_7.3.4a {
dimensions:
   time=2;
   nv=2;

variables:
   double time(time);
      time:standard_name="time" ;
      time:bounds="time_bnds";
      time:units="days since 1961-1-1";
   double time_bnds(time,nv) ;

   float t(time);
      t:long_name="surface air temperature";
      t:cell_methods="area: mean time: mean";
      t:units="K";
      t:comment="Note that area without any X-Y axis implies the whole world, by convention";
      t:comment2="Recommendation: define one size dim-ed variables lon|lat with bounds";

// global attributes:
   :comment="Cell-methods when there is no coordinate" ;
   :Conventions = "CF-1.4" ;

data:

  time= 0.5, 1.5 ;
  time_bnds = 0., 1., 1., 2.;
}
