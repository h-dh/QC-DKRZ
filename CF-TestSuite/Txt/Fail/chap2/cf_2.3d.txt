netcdf cf_2.3d {
dimensions:
   lat = 1;
   lon = 2;

variables:
   float bits+pieces(lat,lon) ;
      bits+pieces:long_name = "mean sea level pressure" ;
      bits+pieces:units = "hPa" ;
      bits+pieces:comment = "invalid character + in variable name" ;

// global attributes:
   :comment="naming convention" ;
   :Conventions = "CF-1.4" ;
}
