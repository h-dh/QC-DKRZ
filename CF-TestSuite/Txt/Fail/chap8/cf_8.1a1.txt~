netcdf cf_8.1a1 {
dimensions:
   lat=2;
   lon=3;

variables:
   float t(lat,lon);
    	 t:scaleFactor = "1";
      t:add_offset = 0.f;
      t:comment= "invalid: scaleFactor and add_offset must be the same type" ;

// global attributes:
   :comment="packed data" ;
   :Conventions = "CF-1.4" ;
}