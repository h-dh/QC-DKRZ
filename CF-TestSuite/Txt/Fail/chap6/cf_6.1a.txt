netcdf cf_6.1a {
dimensions:
   parcel = 2 ; // number of trajectories
   times = 3 ;
   max_len_parcel_name = 5 ; // max length of trajectory name

variables:
   float times(times) ;
      times:standard_name = "time" ;
      times:units="days since 1961-1-1";
   float lon(parcel,times) ;
      lon:standard_name = "longitude";
      lon:units= "degrees_east" ;
   float lat(parcel,times) ;
      lat:standard_name = "latitude";
      lat:units= "degrees_north" ;
   char parcel_name(parcel,max_len_parcel_name) ;

   float rh(parcel,times) ;
      rh:comment2 = "ordering of dimensions is valid, because times is not unlimited" ;
      rh:long_name = "relative humidity" ;
      rh:coordinates = "lat lon" ;
      rh:comment = "label parcel_name is missing in coordinates" ;

// global attributes:
   :comment="Strictness of Chap6 (CF-1.4) is ignored in Appendix H (CF-1.6)" ;
   :Conventions = "CF-1.4" ;

data:

times= 1., 2., 3. ;
parcel_name= "asdf" ;
}

